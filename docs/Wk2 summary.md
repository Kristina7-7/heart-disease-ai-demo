I trained and compared Logistic Regression and Random Forest models using the UCI Heart Disease dataset.

Logistic Regression achieved an accuracy of 54.1%, while Random Forest achieved 50.8%.

This experiment shows that a more complex model does not always perform better. Model performance depends on the dataset, data quality, and class distribution.
